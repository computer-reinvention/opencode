Stage creation of a NEW symbol (function/method/class) in an indexed file. The body is generated from `note` when patches are applied. Use anchor/file to control placement. A created symbol nothing references yet is reported as an orphan advisory — wire it in with a companion trie_patch on its caller. Applied by trie_patch_apply. For new non-Python or non-symbol files, use the backup fs_write tool.
