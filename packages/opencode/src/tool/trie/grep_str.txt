Regex search over indexed source bodies, with each match attributed to its enclosing symbol. Use when you know a literal string or code pattern rather than a symbol name. Searches indexed (Python) files only; for other file types use the backup fs_grep tool.
