Find files by name or path glob across the project — the filename search trie's other tools lack. Use for "where is the Dockerfile", "list the TS files", "find all *.config.* files". Searches the whole tree by default (vendored/.trie dirs pruned), mtime-sorted newest first; pass indexed_only to restrict to trie's scope. This makes the backup fs_glob unnecessary for most filename lookups.
